from typing import Tuple

def renderScene(render, shapes: list) -> tuple[int, int, int, int]:
    """
    Renders a list of shapes to the scene

    :param render: SDL renderer
    :type render: SDL_Renderer
    :param shapes: List of shapes to render
    :type shapes: list
    """
    ...